export class Dragoes {
    constructor(
        private id: string,
        private createdAt: string,
        private name: string,
        private type: string,
        private histories: string[],
    ){}

}